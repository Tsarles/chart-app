/**
 * VersionLabel — fixed top-right on every page via AppLayout.
 * v0.3, dark green, small handwritten style.
 */
export function VersionLabel() {
  return (
    <div className="version-label" aria-hidden="true">
      v0.3
    </div>
  );
}
